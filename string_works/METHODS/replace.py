word = "python programming language" # len = 25

print(len(word)) # len = 27 .........it will add the space as a character...so we want to remove it

replace_val = word.replace(" ","")# remove space and add no space....

print(len(replace_val)) # len = 25