def find_char(word):

    len_word = len(word)

    for i in range(0, len(word)):

        first_char = word[0]
        last_char = word[len_word - 1]

    print(f"first_char : {first_char}")
    print(f"last_char : {last_char}")

find_char("TechnologY")

